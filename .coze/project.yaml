name: OpenClaw-Personal-Assistant
version: "1.0.0"
preview:
  type: openclaw
  command:
    - openclaw
    - gateway
    - run
    - --port=5000
  port: 5000
  healthCheck:
    path: /
    intervalSeconds: 10
    timeoutSeconds: 5
