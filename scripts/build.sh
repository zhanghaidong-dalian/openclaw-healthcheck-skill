#!/bin/bash
# OpenClaw 构建脚本（预览服务调用）
set -e
export HOME="/root"
export PATH="/root/.local/share/pnpm:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
cd /workspace/projects
echo "[build] OpenClaw build step skipped (no build required)"
